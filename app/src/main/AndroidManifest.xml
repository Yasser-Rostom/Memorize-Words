<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yasser.memorizewords"
    >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        android:minSdkVersion="22" />
    <application
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"

        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BasicNote">
        <activity android:name="com.yasser.memorizewords.About"></activity>
        <activity
            android:name="com.yasser.memorizewords.WordListing"
            android:parentActivityName="com.yasser.memorizewords.CategoryListing">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.yasser.memorizewords.CategoryListing" />
        </activity>
        <activity
            android:name="com.yasser.memorizewords.NewWordActivity"
            android:parentActivityName="com.yasser.memorizewords.WordListing">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.yasser.memorizewords.WordListing" />
        </activity>
        <activity
            android:name="com.yasser.memorizewords.Splash"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.yasser.memorizewords.CategoryListing"
            android:theme="@style/Theme.BasicNote" />
    </application>

</manifest>